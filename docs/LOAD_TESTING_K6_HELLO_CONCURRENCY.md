# تست بار همزمانی (Concurrency Load Test) با k6 برای Endpoint `/hello`

این مستند راهنمای کامل اجرا و تحلیل تست بار همزمانی (بدون sleep) برای endpoint `/hello` است. این مستند برای گزارش‌دهی به تیم و مرجع بلندمدت طراحی شده است.

---

## 0. آشنایی با k6

### k6 چیست؟

**k6** یک ابزار load testing مدرن و open-source است که توسط [Grafana Labs](https://k6.io/) توسعه داده شده است. k6 برای تست عملکرد و ظرفیت APIها و سرویس‌های وب طراحی شده است.

### ویژگی‌های کلیدی k6

- **نوشتن تست‌ها با JavaScript (ES6 modules)**: تست‌ها را با JavaScript می‌نویسید
- **Virtual Users (VU)**: هر VU یک کاربر مجازی است که تابع `default()` را به صورت موازی اجرا می‌کند
- **متریک‌های جامع**: خروجی‌های مهم شامل `http_req_duration` (latency)، `http_req_failed`، RPS، p95/p99، و error rate
- **Thresholds**: امکان تعریف معیارهای موفقیت/شکست تست
- **سناریوهای مختلف**: پشتیبانی از انواع مختلف تست (stages, constant-arrival-rate, ramping-arrival-rate و ...)

### مفاهیم پایه

#### Virtual User (VU)

هر VU یک کاربر مجازی است که:
- تابع `default()` را به صورت loop اجرا می‌کند
- به صورت موازی با سایر VUها کار می‌کند
- می‌تواند درخواست HTTP ارسال کند، sleep کند، و عملیات دیگر انجام دهد

#### Iteration

هر بار اجرای تابع `default()` یک iteration محسوب می‌شود. تعداد iterationها به تعداد VU و مدت زمان تست بستگی دارد.

#### RPS (Requests Per Second)

نرخ درخواست در ثانیه. این معیار نشان می‌دهد سیستم چند درخواست در ثانیه می‌تواند پردازش کند.

**نکته مهم:** RPS در k6 معمولاً نتیجه این رابطه است:
```
RPS ≈ VUs ÷ iteration_duration
```

اگر `sleep(1)` داشته باشید، هر VU تقریباً 1 درخواست در ثانیه می‌زند. اگر sleep نداشته باشید، هر VU تا حد امکان سریع درخواست می‌زند.

### متریک‌های مهم k6

| متریک | توضیح |
|-------|-------|
| `http_req_duration` | مدت زمان هر درخواست HTTP (از دید کلاینت) |
| `http_req_failed` | نرخ درخواست‌های ناموفق (timeout, connection error و ...) |
| `http_reqs` | تعداد کل درخواست‌ها |
| `iterations` | تعداد کل iterationها |
| `vus` | تعداد Virtual Userهای فعال |
| `p(95)`, `p(99)` | صدک 95 و 99 (95% و 99% درخواست‌ها زیر این مقدار) |

### Thresholds در k6

**Threshold** یعنی "خط قرمز / شرط قبولی تست". شما به k6 می‌گویید:
- اگر متریک X از مقدار/شرط Y بدتر شد، تست Fail حساب شود

**مثال:**
```javascript
thresholds: {
  http_req_failed: ['rate<0.01'],      // کمتر از 1% خطا
  http_req_duration: ['p(95)<200'],    // 95% درخواست‌ها زیر 200ms
}
```

**نکته:** Threshold تست را متوقف نمی‌کند، فقط در پایان بررسی می‌کند که شروط رعایت شده یا نه. اگر رعایت نشده باشد، k6 exit code غیر صفر می‌دهد (برای CI/CD مهم است).

### انواع Executor در k6

k6 از executorهای مختلفی پشتیبانی می‌کند:

1. **`shared-iterations`**: تعداد کل iterationها مشخص است
2. **`per-vu-iterations`**: هر VU تعداد مشخصی iteration اجرا می‌کند
3. **`constant-vus`**: تعداد ثابت VU برای مدت مشخص
4. **`ramping-vus`**: تعداد VU به صورت مرحله‌ای تغییر می‌کند (stages)
5. **`constant-arrival-rate`**: نرخ ثابت درخواست (RPS-based)
6. **`ramping-arrival-rate`**: نرخ درخواست به صورت مرحله‌ای تغییر می‌کند

---

## 1. مقدمه

### هدف تست بار همزمانی

تست بار همزمانی (Concurrency Load Test) به ما کمک می‌کند تا:
- **ظرفیت حداکثری سیستم** را شناسایی کنیم
- **نقطه شکست** (breaking point) را پیدا کنیم
- **رفتار سیستم تحت فشار** را بررسی کنیم
- **محدودیت‌های همزمانی** را شناسایی کنیم

### چرا Endpoint `/hello`؟

Endpoint `/hello` یک endpoint ساده و سریع است که:
- بدون وابستگی به دیتابیس کار می‌کند
- پردازش سنگین ندارد
- برای تست ظرفیت خالص سیستم مناسب است
- نتایج قابل تکرار و قابل مقایسه است

### تفاوت تست همزمانی با تست مبتنی بر RPS

| ویژگی | تست همزمانی (Concurrency) | تست مبتنی بر RPS |
|-------|---------------------------|------------------|
| **کنترل** | تعداد کاربران همزمان (VU) | نرخ درخواست در ثانیه (RPS) |
| **رفتار** | هر VU تا حد امکان سریع درخواست می‌زند | نرخ ثابت درخواست |
| **هدف** | پیدا کردن ظرفیت حداکثری | پیدا کردن ظرفیت پایدار |
| **مناسب برای** | تست فشار و اشباع | تست بار واقعی |

---

## 2. تست‌های مقدماتی

قبل از اجرای تست بار همزمانی (تست شماره 3)، بهتر است با تست‌های ساده‌تر شروع کنید تا:
- مطمئن شوید k6 و سرویس درست کار می‌کنند
- یک baseline برای مقایسه داشته باشید
- با مفاهیم پایه k6 آشنا شوید

### تست 1: Smoke Test (تست دود)

**هدف:** اطمینان از اینکه سرویس در دسترس است و به درستی پاسخ می‌دهد.

**فایل:** `k6/01-hello-smoke.js`

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = { vus: 1, duration: '10s' };

export default function () {
  const res = http.get('http://localhost:8080/hello');
  check(res, { 'status is 200': (r) => r.status === 200 });
  sleep(1);
}
```

**توضیح:**
- **1 VU**: یک کاربر مجازی
- **10s duration**: تست به مدت 10 ثانیه اجرا می‌شود
- **sleep(1)**: بین هر iteration، 1 ثانیه صبر می‌کند
- **نتیجه:** تقریباً 10 درخواست (1 درخواست در ثانیه)

**اجرا:**
```bash
k6 run k6/01-hello-smoke.js
```

**خروجی مورد انتظار:**
- `http_req_failed: 0.00%` (بدون خطا)
- `http_req_duration: avg < 10ms` (latency پایین)
- `checks: 100%` (همه چک‌ها موفق)

**نکته:** اگر این تست موفق باشد، می‌توانید به تست‌های پیشرفته‌تر بروید.

**تحلیل RPS:**
با `sleep(1)`، هر VU تقریباً 1 درخواست در ثانیه می‌زند. پس:
- **1 VU × 1 req/s = ~1 RPS**

این تست برای بررسی دسترسی سرویس است، نه ظرفیت.

### تست 2: Baseline Test با Stages (تست پایه)

**هدف:** گرفتن baseline برای مقایسه با تست‌های بعدی.

**فایل:** `k6/02-hello-stages.js`

```javascript
import http from 'k6/http';
import { check } from 'k6';

export const options = {
  stages: [
    { duration: '10s', target: 100 },   // 10s: افزایش به 100 VU
    { duration: '20s', target: 300 },   // 20s: افزایش به 300 VU
    { duration: '20s', target: 0 },    // 20s: کاهش به 0 VU
  ],
  thresholds: {
    http_req_failed: ['rate<0.01'],           // کمتر از 1% خطا
    http_req_duration: ['p(95)<200'],         // 95% زیر 200ms
  },
};

export default function () {
  const res = http.get('http://localhost:8080/hello');
  check(res, { '200': (r) => r.status === 200 });
  // توجه: sleep وجود ندارد
}
```

**توضیح:**
- **Stages**: بار به صورت مرحله‌ای افزایش می‌یابد
  - مرحله 1: 10 ثانیه تا 100 VU
  - مرحله 2: 20 ثانیه تا 300 VU
  - مرحله 3: 20 ثانیه تا 0 VU (cooldown)
- **بدون sleep**: هر VU تا حد امکان سریع درخواست می‌زند
- **نتیجه:** RPS خود به خود بالا می‌رود (محدود به CPU/Network)

**اجرا:**
```bash
k6 run k6/02-hello-stages.js
```

**تحلیل نتایج:**

| معیار | مقدار مورد انتظار | توضیح |
|-------|-------------------|-------|
| `http_req_failed` | < 1% | نرخ خطا باید پایین باشد |
| `http_req_duration p95` | < 200ms | 95% درخواست‌ها باید زیر 200ms باشند |
| `http_reqs/s` | متغیر | RPS به تعداد VU و latency بستگی دارد |

**نکته مهم:** در این تست، RPS کنترل نمی‌شود. RPS نتیجه تعداد VU و سرعت پاسخ سرویس است. اگر می‌خواهید RPS را کنترل کنید، باید از `constant-arrival-rate` استفاده کنید.

**مقایسه با تست 1:**

| ویژگی | تست 1 (Smoke) | تست 2 (Stages) |
|-------|---------------|----------------|
| **VU** | 1 | 0 → 100 → 300 → 0 |
| **Sleep** | ✅ (1s) | ❌ (بدون sleep) |
| **RPS** | ~1 req/s | متغیر (تا چند هزار) |
| **هدف** | بررسی دسترسی | گرفتن baseline |
| **مدت زمان** | 10s | 50s |

**نتیجه:** تست 2 به شما یک baseline می‌دهد که می‌توانید با تست‌های بعدی مقایسه کنید.

---

## 3. پیش‌نیازها

### نصب k6

برای نصب k6 روی Ubuntu:

```bash
# روش 1: استفاده از APT repository (توصیه می‌شود)
sudo apt-get update
sudo apt-get install -y gnupg ca-certificates curl

sudo rm -f /usr/share/keyrings/k6-archive-keyring.gpg
curl -fsSL https://dl.k6.io/key.gpg | gpg --dearmor | sudo tee /usr/share/keyrings/k6-archive-keyring.gpg > /dev/null
echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" \
  | sudo tee /etc/apt/sources.list.d/k6.list

sudo apt-get update
sudo apt-get install -y k6

# بررسی نصب
k6 version
```

### راه‌اندازی سرویس

برای اجرای تست، سرویس باید در حال اجرا باشد:

```bash
# روش 1: اجرای محلی (توصیه می‌شود برای تست)
make dev-run

# روش 2: اجرای با Docker
make docker-up
```

**نکته:** برای تست دقیق‌تر، توصیه می‌شود از اجرای محلی (`make dev-run`) استفاده کنید تا منابع سیستم به صورت کامل در دسترس باشد.

### روش‌های دیگر نصب k6

```bash
# روش 2: استفاده از Snap
sudo apt update
sudo apt install -y snapd
sudo snap install k6

# روش 3: استفاده از Docker
docker pull grafana/k6
docker run --rm -i grafana/k6 run - <k6/01-hello-smoke.js
```

---

## 4. سیستم تحت تست (System Under Test - SUT)

### چرا مشخصات سیستم مهم است؟

مشخصات سیستم برای تفسیر نتایج تست بسیار مهم است زیرا:
- **CPU** محدودیت اصلی در تست‌های محلی است
- **RAM** در صورت کمبود باعث swap و افزایش latency می‌شود
- **OS** و تنظیمات شبکه بر عملکرد تأثیر می‌گذارد

### جمع‌آوری مشخصات سیستم

برای گزارش و تفسیر نتایج، مشخصات سیستم را جمع‌آوری کنید:

```bash
# اطلاعات سیستم عامل
lsb_release -a || cat /etc/os-release

# اطلاعات CPU (مدل، تعداد هسته/ترد)
lscpu

# اطلاعات RAM (کل/آزاد)
free -h

# اطلاعات دیسک (اختیاری)
df -h

# اطلاعات شبکه (اختیاری)
ip a
```

### جدول مشخصات سیستم (برای گزارش)

| مشخصه | مقدار |
|------|-------|
| **OS** | [خروجی `lsb_release -a`] |
| **CPU Model** | [از `lscpu`] |
| **CPU Cores** | [از `lscpu`] |
| **CPU Threads** | [از `lscpu`] |
| **Total RAM** | [از `free -h`] |
| **Available RAM** | [از `free -h`] |

**مثال:**
```bash
$ lscpu | grep -E "Model name|CPU\(s\)|Thread"
Model name:            Intel(R) Core(TM) i7-8700K CPU @ 3.70GHz
CPU(s):                12
Thread(s) per core:    2

$ free -h
              total        used        free      shared  buff/cache   available
Mem:           16Gi       2.1Gi       8.5Gi       200Mi       5.4Gi        13Gi
```

---

## 5. استراتژی Threshold

### Threshold چیست؟

Threshold در k6 معیارهای موفقیت/شکست تست را تعریف می‌کند. اگر threshold نقض شود، تست به عنوان failed علامت‌گذاری می‌شود.

### چرا Threshold لازم است؟

- **تعریف معیار موفقیت:** مشخص می‌کند چه نتایجی قابل قبول است
- **تشخیص خودکار مشکل:** اگر threshold نقض شود، می‌دانیم سیستم مشکل دارد
- **مقایسه نتایج:** امکان مقایسه نتایج تست‌های مختلف را فراهم می‌کند

### معیارهای مهم

#### 1. نرخ خطا (`http_req_failed`)

```javascript
http_req_failed: ['rate<0.01']  // کمتر از 1% خطا
```

- **معنی:** درصد درخواست‌هایی که با خطا مواجه شده‌اند
- **هدف:** باید نزدیک به صفر باشد (برای endpoint ساده مثل `/hello`)

#### 2. Latency - p95 (`http_req_duration`)

```javascript
http_req_duration: ['p(95)<50']  // 95% درخواست‌ها زیر 50ms
```

- **معنی:** 95% درخواست‌ها باید زیر این مقدار باشند
- **هدف:** برای endpoint ساده، باید بسیار پایین باشد

#### 3. Latency - p99 (`http_req_duration`)

```javascript
http_req_duration: ['p(99)<150']  // 99% درخواست‌ها زیر 150ms
```

- **معنی:** 99% درخواست‌ها باید زیر این مقدار باشند
- **هدف:** نشان‌دهنده بدترین حالت‌ها است

### روش تعیین Threshold: Baseline → Target → Adjustment

#### مرحله A: Baseline گرفتن

ابتدا یک baseline بدون فشار بگیرید:

```bash
# تست با 1 VU برای 30 ثانیه
k6 run --vus 1 --duration 30s k6/03-hello-concurrency-no-sleep.js
```

**نتیجه:** baseline latency را می‌دهد (مثلاً p95 ≈ 2ms)

**نکته:** اگر baseline شما p95 ≈ 2ms است، threshold `p(95)<200ms` خیلی گشاد است و هیچ‌وقت fail نمی‌شود.

#### مرحله B: تعیین هدف کاربردی

دو حالت رایج:

**حالت 1 (محصول‌محور):**
- SLA/هدف محصول مشخص است: مثلاً p95 زیر 50ms، خطا زیر 1%
- از این مقادیر استفاده کنید

**حالت 2 (فقط ظرفیت محلی):**
- هدف: پیدا کردن "نقطه شکست"
- Thresholdها را مثل "خط توقف" تنظیم کنید:
  - `error < 2%`
  - `p95 < 200ms`
- این به معنی "کیفیت" نیست، به معنی "از اینجا به بعد غیرقابل اتکا است"

#### مرحله C: نسبت به منابع سیستم معنادار کردن

روی لوکال معمولاً:
- تا وقتی CPU زیر ~70% است، latency خوب است
- از یک نقطه به بعد CPU اشباع می‌شود و p95/p99 نمایی بالا می‌رود

**پیشنهاد عملی برای شروع:**

برای endpoint ساده مثل `/hello` روی لوکال:

```javascript
thresholds: {
  http_req_failed: ['rate<0.01'],           // 1% خطا
  http_req_duration: ['p(95)<50', 'p(99)<150'],  // p95 < 50ms, p99 < 150ms
}
```

**نکته:** این مقادیر را با داده واقعی خودتان اصلاح کنید.

---

## 5. تست بار همزمانی (بدون Sleep)

### رفتار Virtual User بدون Sleep

وقتی `sleep` حذف می‌شود، هر VU این کار را می‌کند:
- **پشت سر هم درخواست می‌زند** (as fast as possible)
- **بدون توقف** بین درخواست‌ها
- **RPS خود به خود بالا می‌رود** تا محدودیت سیستم

### چرا این تست Contention و Saturation را نشان می‌دهد؟

این تست:
- **CPU را اشباع می‌کند:** تا جایی که CPU به 100% برسد
- **Lock contention را نشان می‌دهد:** اگر lock contention وجود داشته باشد
- **Scheduler pressure را نشان می‌دهد:** فشار روی scheduler سیستم
- **Network stack را تست می‌کند:** فشار روی network stack

### این تست چه چیزی را اندازه‌گیری می‌کند و چه چیزی را نمی‌کند؟

**اندازه‌گیری می‌کند:**
- ✅ ظرفیت حداکثری سیستم (maximum capacity)
- ✅ نقطه شکست (breaking point)
- ✅ رفتار سیستم تحت فشار خالص
- ✅ محدودیت‌های CPU و scheduler

**اندازه‌گیری نمی‌کند:**
- ❌ رفتار واقعی کاربران (که بین درخواست‌ها فاصله دارند)
- ❌ ظرفیت پایدار (sustained capacity)
- ❌ رفتار سیستم تحت بار واقعی

**خلاصه:** این تست می‌گوید "در حالت فشار خالص و غیرواقعی، سقف throughput+concurrency این سیستم چقدر است؟"

### چرا این تست مهم است؟

این تست (تست شماره 3) ادامه منطقی تست‌های قبلی است:
- **تست 1 (Smoke)**: اطمینان از دسترسی سرویس
- **تست 2 (Stages)**: گرفتن baseline با ramp-up ساده
- **تست 3 (Concurrency No Sleep)**: پیدا کردن ظرفیت حداکثری با ramp-up دقیق‌تر

**تفاوت با تست 2:**
- تست 2: ramp-up سریع (10s → 100 VU)
- تست 3: ramp-up تدریجی‌تر (10s → 50, 20s → 100, 20s → 200, 20s → 300)
- تست 3: thresholdهای دقیق‌تر (p95 < 50ms, p99 < 150ms)

---

## 7. توضیح اسکریپت تست

اسکریپت تست در فایل `k6/03-hello-concurrency-no-sleep.js` قرار دارد:

```javascript
import http from 'k6/http';
import { check } from 'k6';

export const options = {
  stages: [
    { duration: '10s', target: 50 },   // 10 ثانیه: افزایش به 50 VU
    { duration: '20s', target: 100 },   // 20 ثانیه: افزایش به 100 VU
    { duration: '20s', target: 200 },   // 20 ثانیه: افزایش به 200 VU
    { duration: '20s', target: 300 },   // 20 ثانیه: افزایش به 300 VU
    { duration: '10s', target: 0 },     // 10 ثانیه: کاهش به 0 VU (cooldown)
  ],
  thresholds: {
    http_req_failed: ['rate<0.01'],           // کمتر از 1% خطا
    http_req_duration: ['p(95)<50', 'p(99)<150'],  // p95 < 50ms, p99 < 150ms
  },
};

export default function () {
  const res = http.get('http://localhost:8080/hello');
  check(res, { 'status 200': (r) => r.status === 200 });
  // توجه: sleep وجود ندارد - هر VU تا حد امکان سریع درخواست می‌زند
}
```

### توضیح Configuration

#### Stages (مراحل افزایش بار)

| مرحله | مدت زمان | تعداد VU | توضیح |
|-------|----------|----------|-------|
| 1 | 10s | 0 → 50 | گرم‌کردن (warm-up) |
| 2 | 20s | 50 → 100 | افزایش بار |
| 3 | 20s | 100 → 200 | افزایش بیشتر |
| 4 | 20s | 200 → 300 | افزایش تا حداکثر |
| 5 | 10s | 300 → 0 | خنک‌کردن (cooldown) |

**کل زمان تست:** 80 ثانیه

#### Thresholds

- **`http_req_failed < 1%`:** نرخ خطا باید کمتر از 1% باشد
- **`p95 < 50ms`:** 95% درخواست‌ها باید زیر 50ms باشند
- **`p99 < 150ms`:** 99% درخواست‌ها باید زیر 150ms باشند

**نکته:** این مقادیر را با توجه به سیستم و baseline خودتان تنظیم کنید.

---

## 8. نحوه اجرای تست

### دستور اجرا

```bash
k6 run k6/03-hello-concurrency-no-sleep.js
```

### چه چیزی را در حین اجرا مانیتور کنیم؟

#### 1. CPU و RAM سیستم

در یک terminal جداگانه:

```bash
# مانیتورینگ کلی سیستم
htop

# یا مانیتورینگ CPU/RAM
watch -n 1 'top -bn1 | head -20'
```

**چیزهایی که باید بررسی کنید:**
- **CPU Usage:** آیا CPU به 100% می‌رسد؟
- **CPU per Process:** بیشتر CPU روی k6 است یا سرویس Go؟
- **RAM Usage:** آیا RAM/Swap درگیر است؟ (اگر بله، latency بالا می‌رود)

#### 2. مانیتورینگ دقیق‌تر Process

```bash
# پیدا کردن PID
pidof k6
pidof go-backend-service  # یا نام binary شما

# مانیتورینگ یک process خاص
pidstat -p <PID> 1
```

#### 3. خروجی k6

در terminal که k6 در حال اجرا است، به این موارد توجه کنید:
- **RPS (http_reqs/s):** آیا RPS دیگر رشد نمی‌کند؟
- **Latency (p95/p99):** آیا latency ناگهان بالا می‌رود؟
- **Error rate:** آیا error rate شروع به افزایش می‌کند؟

### مثال خروجی

```
     ✓ status 200
     ✗ http_req_duration: p(95)<50
     ✗ http_req_duration: p(99)<150

     checks.........................: 100.00% ✓ 125000    ✗ 0
     data_received..................: 2.1 MB  26 kB/s
     data_sent......................: 15 MB   188 kB/s
     http_req_duration..............: avg=45.2ms min=0.5ms med=2.1ms max=850ms p(90)=120ms p(95)=180ms p(99)=450ms
     http_req_failed................: 0.00%   ✓ 0         ✗ 0
     http_reqs.....................: 125000  1562.5/s
     iteration_duration.............: avg=45.3ms min=0.5ms med=2.1ms max=850ms
     vus............................: 300     min=0       max=300
     vus_max........................: 300     min=0       max=300
```

---

## 9. شناسایی حد ظرفیت (Capacity Limit)

### چگونه حد ظرفیت را شناسایی کنیم؟

حد ظرفیت جایی است که سیستم دیگر نمی‌تواند بار بیشتری را تحمل کند. این را می‌توان از چند نشانه تشخیص داد:

### 1. رشد Latency (Latency Growth)

**نشانه:**
- p95/p99 به صورت **نمایی** بالا می‌رود
- از یک نقطه به بعد، latency چند برابر می‌شود

**مثال:**
```
VU=100: p95=5ms,  p99=10ms
VU=200: p95=15ms, p99=30ms
VU=300: p95=180ms, p99=450ms  ← اینجا سیستم کم آورده
```

**نکته:** اگر p95 از 50ms به 200ms می‌رود، احتمالاً به حد ظرفیت رسیده‌اید.

### 2. نرخ خطا (Error Rate)

**نشانه:**
- `http_req_failed` شروع به افزایش می‌کند
- خطاهای timeout یا connection error ظاهر می‌شوند

**مثال:**
```
VU=100: http_req_failed=0.00%
VU=200: http_req_failed=0.01%
VU=300: http_req_failed=2.5%  ← اینجا مشکل داریم
```

### 3. سقف RPS (RPS Plateau)

**نشانه:**
- RPS دیگر رشد نمی‌کند
- با افزایش VU، RPS ثابت می‌ماند یا حتی کاهش می‌یابد
- اما latency و errors بالا می‌روند

**مثال:**
```
VU=100:  RPS=2000/s, p95=5ms
VU=200:  RPS=3500/s, p95=15ms
VU=300:  RPS=3500/s, p95=180ms  ← RPS دیگر رشد نمی‌کند
VU=400:  RPS=3400/s, p95=450ms  ← حتی کاهش یافته
```

**نکته:** اگر RPS دیگر رشد نمی‌کند ولی VU افزایش می‌یابد، به سقف رسیده‌اید.

### 4. اشباع CPU (CPU Saturation)

**نشانه:**
- CPU به 100% می‌رسد
- CPU دیگر نمی‌تواند بیشتر کار کند

**بررسی:**
```bash
# در terminal جداگانه
htop
# یا
top
```

**نکته:** اگر CPU کلاینت (k6) به 100% رسید، ممکن است محدودیت از سمت k6 باشد نه سرویس.

### خلاصه: معیارهای شناسایی حد ظرفیت

| معیار | نشانه حد ظرفیت |
|-------|----------------|
| **Latency** | p95/p99 به صورت نمایی بالا می‌رود |
| **Error Rate** | شروع به افزایش می‌کند (>1%) |
| **RPS** | دیگر رشد نمی‌کند (plateau) |
| **CPU** | به 100% می‌رسد |

**نکته مهم:** اگر همه این نشانه‌ها همزمان ظاهر شوند، به وضوح به حد ظرفیت رسیده‌اید.

---

## 10. نتایج - ظرفیت حداکثری مشاهده شده

### نتایج تست‌های انجام شده

در این بخش نتایج دو تست مختلف با configurationهای متفاوت ارائه شده است:

#### تست 1: Ramp-up تا 300 VU

**Configuration:**
```javascript
stages: [
  { duration: '10s', target: 50 },   // 10s: افزایش به 50 VU (warm-up)
  { duration: '20s', target: 100 },  // 20s: افزایش به 100 VU
  { duration: '20s', target: 200 },  // 20s: افزایش به 200 VU
  { duration: '20s', target: 300 },  // 20s: افزایش به 300 VU
  { duration: '10s', target: 0 },     // 10s: کاهش به 0 VU (cooldown)
]
```

**سیستم تحت تست:**
- **CPU:** Intel i7-7700 (8 threads)
- **RAM:** 16GB
- **OS:** Ubuntu 24.04
- **اجرا:** محلی (local)

**نتایج:**

| معیار | مقدار | وضعیت |
|-------|------|-------|
| **حداکثر VU** | 299 | - |
| **RPS متوسط** | ~11,104 req/s | ✅ |
| **کل درخواست‌ها** | 888,453 | - |
| **مدت زمان** | 80s (1m20s) | - |
| **p95 latency** | 38.47ms | ✅ زیر threshold (50ms) |
| **p99 latency** | 65.66ms | ✅ زیر threshold (150ms) |
| **avg latency** | 12.05ms | ✅ |
| **median latency** | 7.61ms | ✅ |
| **max latency** | 203.58ms | ⚠️ |
| **نرخ خطا** | 0.00% | ✅ |
| **CPU Usage** | 100% | ⚠️ اشباع شده |

**Thresholds:**

| Threshold | مقدار واقعی | وضعیت |
|-----------|-------------|-------|
| `p(95)<50` | 38.47ms | ✅ Pass |
| `p(99)<150` | 65.66ms | ✅ Pass |
| `rate<0.01` | 0.00% | ✅ Pass |

**تفسیر:**
- ✅ سیستم در فشار بالا سالم می‌ماند (0% error)
- ✅ Latency در محدوده قابل قبول است (p95 < 50ms)
- ⚠️ CPU به 100% رسیده، پس این RPS احتمالاً "سقف توان این ماشین" است
- 📊 این عدد (~11k RPS) بیشتر "سقف توان این ماشین در این تست" است تا "سقف توان سرویس"

**نتیجه:** تست **Pass** شد - همه thresholdها رعایت شدند.

---

#### تست 2: Ramp-up تا 600 VU

**Configuration:**
```javascript
stages: [
  { duration: '10s', target: 100 },   // 10s: افزایش به 100 VU
  { duration: '20s', target: 200 },    // 20s: افزایش به 200 VU
  { duration: '20s', target: 400 },   // 20s: افزایش به 400 VU
  { duration: '20s', target: 600 },   // 20s: افزایش به 600 VU
  { duration: '10s', target: 0 },     // 10s: کاهش به 0 VU (cooldown)
]
```

**سیستم تحت تست:**
- **CPU:** Intel i7-7700 (8 threads)
- **RAM:** 16GB
- **OS:** Ubuntu 24.04
- **اجرا:** محلی (local)

**نتایج:**

| معیار | مقدار | وضعیت |
|-------|------|-------|
| **حداکثر VU** | 600 | - |
| **RPS متوسط** | ~13,055 req/s | ✅ (18% بیشتر از تست 1) |
| **p95 latency** | 65.83ms | ❌ از threshold رد شده (50ms) |
| **p99 latency** | 114.77ms | ✅ زیر threshold (150ms) |
| **avg latency** | 20.15ms | ⚠️ (67% بیشتر از تست 1) |
| **median latency** | 12.6ms | ✅ |
| **max latency** | ~286ms | ⚠️ |
| **نرخ خطا** | 0.00% | ✅ |
| **CPU Usage** | 100% | ⚠️ اشباع شده |

**Thresholds:**

| Threshold | مقدار واقعی | وضعیت |
|-----------|-------------|-------|
| `p(95)<50` | 65.83ms | ❌ **Fail** (15.83ms بیشتر) |
| `p(99)<150` | 114.77ms | ✅ Pass |
| `rate<0.01` | 0.00% | ✅ Pass |

**تفسیر:**

**مقایسه با تست 1:**

| معیار | تست 1 (300 VU) | تست 2 (600 VU) | تغییر |
|-------|----------------|----------------|-------|
| **RPS** | ~11.1k | ~13.1k | +18% ✅ |
| **p95** | 38.47ms | 65.83ms | +71% ❌ |
| **p99** | 65.66ms | 114.77ms | +75% ⚠️ |
| **avg** | 12.05ms | 20.15ms | +67% ⚠️ |
| **Error** | 0% | 0% | بدون تغییر ✅ |

**تحلیل:**

1. **افزایش RPS:** با دو برابر کردن VU (300 → 600)، RPS فقط 18% افزایش یافت. این نشان می‌دهد:
   - سیستم به سقف throughput نزدیک شده
   - هر VU اضافی سود کمتری دارد

2. **افزایش Latency:** p95 از 38ms به 66ms رسید (71% افزایش):
   - این الگوی کلاسیک "اشباع CPU / contention" است
   - بعد از یک نقطه، هر VU اضافی بیشتر latency را افزایش می‌دهد تا RPS

3. **نقطه کیفیت:** با threshold `p95<50ms`:
   - ظرفیت قابل قبول جایی بین 300 تا 600 VU است
   - احتمالاً بین 400 تا 600 VU نقطه عبور رخ داده

4. **سیستم هنوز سالم است:**
   - 0% error rate
   - همه درخواست‌ها با status 200 پاسخ داده‌اند
   - فقط کیفیت latency (p95) از استاندارد تعیین شده عبور کرده

**نتیجه:** تست **Fail** شد - threshold `p(95)<50` نقض شد.

**پیام k6:**
```
thresholds on metrics 'http_req_duration' have been crossed
```

**خلاصه یک‌خطی برای گزارش تیمی:**

> On local Ubuntu 24.04 (i7-7700, 16GB RAM), `/hello` sustained ~13.1k RPS at 600 max VUs with 0% errors; however p95 latency degraded to ~66ms (threshold p95<50ms crossed).

---

### مقایسه دو تست

| ویژگی | تست 1 (300 VU) | تست 2 (600 VU) |
|-------|----------------|----------------|
| **حداکثر VU** | 300 | 600 |
| **RPS** | ~11.1k | ~13.1k (+18%) |
| **p95** | 38.47ms ✅ | 65.83ms ❌ |
| **p99** | 65.66ms ✅ | 114.77ms ✅ |
| **Error Rate** | 0% ✅ | 0% ✅ |
| **Threshold Status** | ✅ Pass | ❌ Fail (p95) |
| **CPU** | 100% | 100% |

**نتیجه‌گیری:**

1. **ظرفیت پایدار (با کیفیت):** تا 300 VU، سیستم پایدار است و همه thresholdها pass می‌شوند
2. **نقطه شکست کیفیت:** بین 300 تا 600 VU، p95 از threshold رد می‌شود
3. **حداکثر ظرفیت:** در 600 VU، RPS به ~13k می‌رسد اما کیفیت latency کاهش می‌یابد
4. **محدودیت:** CPU در هر دو تست به 100% می‌رسد، پس bottleneck احتمالاً CPU است

**توصیه:**

- برای **کیفیت پایدار:** حداکثر 300 VU (یا کمتر)
- برای **حداکثر throughput:** تا 600 VU (با قبول کاهش کیفیت latency)
- برای **نتایج دقیق‌تر:** k6 را روی ماشین جداگانه اجرا کنید

---

### جدول نتایج (برای پر کردن بعد از اجرا)

| معیار | مقدار | توضیح |
|-------|------|-------|
| **حداکثر VU موفق** | | آخرین VU که همه thresholdها pass شدند |
| **حداکثر RPS** | | بیشترین RPS مشاهده شده |
| **p95 در حداکثر VU** | | p95 latency در حداکثر VU موفق |
| **p99 در حداکثر VU** | | p99 latency در حداکثر VU موفق |
| **نرخ خطا در حداکثر VU** | | درصد خطا در حداکثر VU موفق |
| **CPU Usage در حداکثر VU** | | درصد استفاده CPU (سرویس + k6) |
| **نقطه شکست (VU)** | | اولین VU که thresholdها fail شدند |
| **RPS در نقطه شکست** | | RPS در نقطه شکست |
| **p95 در نقطه شکست** | | p95 latency در نقطه شکست |
| **p99 در نقطه شکست** | | p99 latency در نقطه شکست |

**نکته:** برای جزئیات کامل نتایج دو تست انجام شده، به بخش "نتایج تست‌های انجام شده" در بالا مراجعه کنید.

---

## 11. نکات و محدودیت‌ها

### محدودیت‌های تست محلی

#### 1. محدودیت منابع سیستم

وقتی هم k6 و هم سرویس Go روی یک سیستم اجرا می‌شوند:
- **CPU مشترک:** k6 و سرویس برای CPU رقابت می‌کنند
- **RAM مشترک:** اگر RAM کم باشد، swap فعال می‌شود و latency بالا می‌رود
- **Network stack مشترک:** network stack ممکن است bottleneck شود

**راه حل:** برای نتایج دقیق‌تر، k6 را روی سیستم جداگانه اجرا کنید.

#### 2. محدودیت سمت کلاینت (k6)

ممکن است قبل از اینکه سرویس کم بیاورد، **خود k6** محدود شود:
- CPU کلاینت به 100% می‌رسد
- Network stack کلاینت اشباع می‌شود

**شناسایی:** اگر CPU کلاینت (k6) به 100% رسید ولی CPU سرویس زیر 100% است، محدودیت از سمت k6 است.

**راه حل:**
- k6 را روی سیستم قوی‌تر اجرا کنید
- یا از distributed k6 استفاده کنید

#### 3. تست غیرواقعی

این تست (بدون sleep) **غیرواقعی** است:
- کاربران واقعی بین درخواست‌ها فاصله دارند
- این تست فقط "ظرفیت حداکثری" را نشان می‌دهد، نه "ظرفیت پایدار"

**راه حل:** برای تست واقعی‌تر، از تست‌های RPS-based استفاده کنید (مثل `constant-arrival-rate`).

### محدودیت‌های دیگر

#### 4. Network Latency

در تست محلی (`localhost`):
- Network latency تقریباً صفر است
- در production، network latency تأثیر می‌گذارد

#### 5. سایر سرویس‌ها

در تست محلی:
- فقط endpoint `/hello` تست می‌شود
- در production، سایر endpoint‌ها و سرویس‌ها هم بار دارند

### توصیه‌ها

1. **برای تست دقیق‌تر:** k6 را روی سیستم جداگانه اجرا کنید
2. **برای تست واقعی‌تر:** از تست‌های RPS-based استفاده کنید
3. **برای تست production:** از محیط production یا staging استفاده کنید
4. **برای مقایسه:** همیشه مشخصات سیستم را در گزارش ذکر کنید

---

## 12. تفسیر نتایج واقعی

**نکته:** برای مشاهده نتایج کامل دو تست انجام شده (300 VU و 600 VU)، به بخش [10. نتایج - ظرفیت حداکثری مشاهده شده](#10-نتایج---ظرفیت-حداکثری-مشاهده-شده) مراجعه کنید.

### چرا Latency با وجود endpoint ساده بالا است؟

با اینکه endpoint `/hello` بسیار ساده است، در تست‌های با VU بالا، avg latency افزایش می‌یابد. دلایل احتمالی:

1. **CPU اشباع شده:** وقتی CPU به 100% می‌رسد، context switching زیاد می‌شود
2. **Scheduler pressure:** Go runtime scheduler تحت فشار قرار می‌گیرد
3. **Network stack:** حتی localhost هم تحت فشار قرار می‌گیرد
4. **Queueing:** درخواست‌ها در صف می‌مانند تا CPU آزاد شود

**نکته:** این طبیعی است. در فشار بالا، latency افزایش می‌یابد حتی برای endpoint‌های ساده.

### الگوی کلاسیک اشباع CPU

از مقایسه دو تست می‌توان الگوی کلاسیک "اشباع CPU / contention" را مشاهده کرد:

1. **تا یک نقطه:** RPS و latency هر دو خوب هستند
2. **بعد از آن نقطه:** RPS دیگر رشد نمی‌کند (یا رشد کمی دارد)
3. **اما Latency:** به صورت نمایی افزایش می‌یابد

**مثال از تست‌های انجام شده:**
- تست 1 (300 VU): RPS ~11.1k، p95 = 38ms ✅
- تست 2 (600 VU): RPS ~13.1k (+18%)، p95 = 66ms ❌

**نتیجه:** دو برابر کردن VU فقط 18% RPS اضافه کرد، اما p95 را 71% افزایش داد.

### مراحل بعدی

1. **تست با VU بیشتر:** همین تست را با `target: 800` یا `target: 1000` اجرا کنید تا ببینید:
   - آیا RPS بالاتر می‌رود یا همان حوالی 13k سقف می‌زند؟
   - آیا p95/p99 از threshold رد می‌کند؟

2. **تست RPS-based:** از `constant-arrival-rate` استفاده کنید تا دقیق مشخص شود:
   - "چه RPS پایداری را می‌توانی تضمین کنی" (نه فقط max burst)

3. **تست روی ماشین جدا:** k6 را روی ماشین جداگانه اجرا کنید تا محدودیت CPU کلاینت حذف شود

---

## خلاصه

این مستند راهنمای کامل تست بار همزمانی برای endpoint `/hello` است. با دنبال کردن این راهنما می‌توانید:

- ✅ با مفاهیم پایه k6 آشنا شوید
- ✅ تست‌های مقدماتی (Smoke و Baseline) را اجرا کنید
- ✅ تست بار همزمانی را اجرا و تحلیل کنید
- ✅ نتایج دو تست مختلف را مقایسه کنید
- ✅ ظرفیت حداکثری سیستم را شناسایی کنید
- ✅ نقطه شکست را پیدا کنید
- ✅ نتایج را به صورت حرفه‌ای گزارش دهید
- ✅ محدودیت‌های سیستم را درک کنید

**نکته نهایی:** همیشه نتایج را با توجه به مشخصات سیستم و محیط تست تفسیر کنید. نتایج تست محلی ممکن است با production متفاوت باشد.

---

## منابع و مراجع

- [k6 Documentation](https://k6.io/docs/)
- [k6 Thresholds](https://k6.io/docs/using-k6/thresholds/)
- [k6 Stages](https://k6.io/docs/using-k6/scenarios/stages/)
- [Load Testing Best Practices](https://k6.io/docs/test-types/)

# تست k6 با نرخ ثابت (Constant-Arrival-Rate) برای `/hello` — ظرفیت پایدار سیستم

این سند توضیح می‌دهد تست **constant-arrival-rate** در k6 چه چیزی را می‌سنجد، خروجی‌ها را چگونه باید تفسیر کرد، و چطور با تنظیم پارامترها **ظرفیت پایدار** (Sustained Capacity) را دقیق‌تر به‌دست بیاورید.

> سند مرتبط (تست همزمانی بدون sleep): `docs/LOAD_TESTING_K6_HELLO_CONCURRENCY.md`

---

## 1) چرا constant-arrival-rate؟

تست **همزمانی (Concurrency / no-sleep)** به شما می‌گوید:
- «اگر کاربرها تا جایی که می‌توانند سریع درخواست بزنند، سیستم تا کجا بالا می‌آید؟»

تست **constant-arrival-rate** به شما می‌گوید:
- «آیا سیستم می‌تواند یک **نرخ مشخص** (RPS) را **پایدار** نگه دارد، در حالی که latency/error در محدوده قابل قبول بماند؟»

این روش معمولاً شبیه‌تر به ترافیک واقعی است، چون ورودی را با نرخ ثابت مدل می‌کند.

---

## 2) اسکریپت تست: `k6/04-hello-constant-arrival-rate.js`

در این تست یک سناریو با executor `constant-arrival-rate` اجرا می‌شود:
- k6 تلاش می‌کند دقیقاً **rate** iteration در هر **timeUnit** اجرا کند.
- اگر هر iteration فقط یک HTTP request داشته باشد، `rate` تقریباً برابر با **RPS** است.
- اگر پاسخ‌ها کند شوند، k6 برای حفظ نرخ، تعداد VUها را بالا می‌برد (تا سقف `maxVUs`).

---

## 3) معنی پارامترهای کلیدی (خیلی مهم)

### 3.1 `rate`
**هدف نرخ ورودی** است: تعداد iteration در ثانیه (≈ RPS).  
مثلاً `rate: 20000` یعنی «هدف حدود ۲۰هزار درخواست در ثانیه».

### 3.2 `duration`
مدت زمان اجرای تست با آن نرخ هدف.  
- ۳۰ ثانیه فقط یک snapshot سریع می‌دهد.
- ۲ تا ۵ دقیقه ظرفیت پایدار را بهتر نشان می‌دهد (GC، تجمع فشار، exporterها، ...).

### 3.3 `preAllocatedVUs`
تعداد VUهایی که k6 **از قبل** آماده می‌کند.  
- اگر خیلی کم باشد، وسط تست مجبور می‌شود VU جدید بسازد (نوسان/هزینه).
- اگر خیلی زیاد باشد، بی‌خودی منابع (CPU/RAM) مصرف می‌شود.

### 3.4 `maxVUs`
حداکثر VU مجاز برای سناریو.  
- اگر latency بالا برود، k6 برای نگه داشتن نرخ هدف، VU بیشتری فعال می‌کند.
- اگر به `maxVUs` برسد و هنوز کافی نباشد، k6 **نمی‌تواند نرخ هدف را حفظ کند** و معمولاً `dropped_iterations` و هشدار کمبود VU دیده می‌شود.

---

## 4) تحلیل نتیجه تست شما (هدف 20k RPS برای 2 دقیقه)

### 4.1 خلاصه اجرا
فرمان:
```bash
k6 run 04-hello-constant-arrival-rate.js
```

پیکربندی (طبق کانفیگ اولیه اسکریپت ):
- executor: `constant-arrival-rate`
- rate: **20000 /s**
- duration: **2m**
- preAllocatedVUs: **300**
- maxVUs: **1000**

### 4.2 هشدار مهم: کمبود VU (رسیدن به سقف maxVUs)
در خروجی:
```
WARN Insufficient VUs, reached 1000 active VUs and cannot initialize more
```
معنی:
- برای نگه داشتن 20k RPS، k6 نیاز داشته VUهای بیشتری فعال کند.
- اما به سقف `maxVUs=1000` رسیده و دیگر اجازه افزایش ندارد.
- پس **نرخ هدف 20k به‌طور کامل محقق نشده**.

### 4.3 آیا به نرخ هدف رسیدیم؟ (Achieved RPS)
خروجی:
- `http_reqs: 2264270 18865.69/s`

یعنی:
- نرخ واقعی/میانگین حدود **18.9k RPS** بوده، نه 20k.
- اختلاف حدود **~5.7%** کمتر از هدف است.

### 4.4 dropped_iterations یعنی چی و چرا مهم است؟
خروجی:
- `dropped_iterations: 135700 (≈ 1130.64/s)`

معنی دقیق:
- عملا k6 می‌خواسته 20k iteration/s اجرا کند.
- ولی چون VU کافی نداشته (به سقف `maxVUs` خورده)، بخشی از iterationها **اجرا نشده** و dropped شده‌اند.
- این شاخص یکی از دقیق‌ترین علامت‌ها برای این است که:
  - «با محدودیت فعلی VU (یا منابع)، این نرخ قابل حفظ نیست.»

### 4.5 Latency و Error
با وجود نرسیدن کامل به نرخ هدف، کیفیت پاسخ‌ها خوب بوده:
- error: `0%` (هیچ fail نداشتید)
- `p95 = 46.15ms` (زیر 100ms) ✅
- `p99 = 77.53ms` (زیر 250ms) ✅
- `avg = 14.75ms`, `median = 9.41ms`, `max ≈ 304ms`

نتیجه:
- سرویس از نظر correctness پایدار بوده.
- اما از نظر «تحقق نرخ هدف»، محدودیت VU/منابع سمت load generator یا کل سیستم باعث شده به 20k کامل نرسد.

### 4.6 VU usage
- `vus max=981`
- `vus_max=1000`

یعنی k6 تقریباً به سقف 1000 رسیده؛ این کاملاً با هشدار کمبود VU هم‌خوان است.

---

## 5) این تست دقیقاً چه چیزی به شما می‌گوید؟

### می‌گوید:
- روی همین ماشین و همین شرایط، سیستم توانسته حدود **18.9k RPS** را به مدت 2 دقیقه پایدار نگه دارد، با:
  - error = 0%
  - p95 ≈ 46ms
  - p99 ≈ 78ms

### و نشان می‌دهد:
- نرخ هدف **20k** با سقف فعلی `maxVUs=1000` قابل حفظ نبوده (وجود dropped_iterations و هشدار).

پس یک جمله گزارش‌پذیر:
> «در constant-arrival-rate با هدف 20k RPS، نرخ واقعی ~18.9k RPS achieved شد؛ latency خوب و error صفر بود، اما به دلیل رسیدن به سقف maxVUs، بخشی از iterationها dropped شدند.»

---

## 6) برای به‌دست آوردن ظرفیت "دقیق‌تر" چه کار کنیم؟

### گزینه A (ساده‌ترین): maxVUs را کمی افزایش بده
چون به سقف خوردی، برای تست‌های نرخ‌های بالاتر:
- `maxVUs` را بالاتر ببر (مثلاً 1500 یا 2000)
- `preAllocatedVUs` را هم متناسب کمی بالا ببر (مثلاً 500) تا k6 وسط تست کمتر VU بسازد.

هدف:
- اگر با maxVUs بالاتر **dropped_iterations صفر شد** و نرخ هدف محقق شد، یعنی قبلاً محدودیت از سمت VU بوده.

### گزینه B: نرخ را sweep کن تا نقطه شکست را دقیق پیدا کنی
به‌جای پریدن مستقیم به 20k، پله‌ای برو:
- 14k → 16k → 18k → 19k → 20k
هر کدام 2 دقیقه، و این‌ها را ثبت کن:
- achieved RPS
- dropped_iterations
- p95/p99
- CPU سرور و CPU k6

نقطه‌ای که:
- dropped_iterations شروع می‌شود
یا
- p95/p99 از معیار شما رد می‌کند
می‌شود مرز ظرفیت پایدار در آن معیار کیفیت.

### گزینه C (بهترین برای "ظرفیت واقعی سرویس"): k6 را روی یک ماشین جدا اجرا کن
اگر k6 و سرویس روی یک سیستم باشند، سر CPU با هم رقابت می‌کنند و نتیجه می‌تواند ظرفیت واقعی سرویس را کمتر/متفاوت نشان دهد.
پس:
- سرویس روی سیستم A
- k6 روی سیستم B (ترجیحاً LAN با تاخیر کم)

---

## 7) پیشنهاد تنظیمات برای ادامه

### 7.1 تنظیمات پیشنهادی برای تکرار همین تست (20k)
اگر می‌خواهی واقعاً ببینی 20k قابل حفظ است یا نه:
- `preAllocatedVUs: 500`
- `maxVUs: 2000`
- `duration: 2m` (همان)

و بعد نگاه کن:
- آیا achieved RPS ≈ 20k می‌شود؟
- آیا dropped_iterations نزدیک صفر می‌شود؟

### 7.2 معیارهای تشخیص ظرفیت پایدار
یک نرخ را «پایدار» حساب کن اگر:
- `dropped_iterations == 0` (یا تقریباً صفر)
- `http_req_failed` زیر حد شما
- `p95/p99` زیر حد شما
- CPU/Memory به حالت غیرعادی (swap/oom) نرود

---

## 8) قالب گزارش (برای تیم)

برای هر run این‌ها را پر کن:

- **Endpoint:** `/hello`
- **Test type:** constant-arrival-rate
- **Target rate:** ____ RPS
- **Duration:** ____
- **Achieved RPS:** ____
- **Dropped iterations:** ____ (____ /s)
- **Errors:** ____ %
- **Latency:** avg=____, p95=____, p99=____, max=____
- **VU usage:** max active VUs=____, maxVUs=____, preAllocatedVUs=____
- **CPU (server):** ____ %
- **CPU (k6):** ____ %
- **Notes:** ____

---


## 📌 تحلیل تست Constant-Arrival-Rate با نرخ 21,000 RPS (Stable Capacity Candidate)


---

### 🔧 تنظیمات تست
- **Executor:** constant-arrival-rate
- **Target rate:** 21,000 iteration/s (≈ 21k RPS)
- **Duration:** 2 دقیقه
- **preAllocatedVUs:** 700
- **maxVUs:** 2000
- **محیط اجرا:** Local (Ubuntu 24.04 – i7-7700 – 16GB RAM)

---

### 📊 آیا نرخ هدف محقق شد؟
خیر، ولی بسیار نزدیک به آن.

- **Achieved RPS:** ~20,373 req/s  
- **اختلاف با هدف:** حدود 3%
- این اختلاف نسبت به تست 22k کمتر شده و نشان‌دهنده نزدیک شدن به نقطه پایدار است.

---

### ⚠️ dropped_iterations (شاخص کلیدی)
- **Dropped iterations:** 74,739  
- **Dropped / sec:** ~623 iteration/s

**تفسیر:**
- سیستم (یا load generator) هنوز قادر نیست 21k RPS را کاملاً و بدون افت نگه دارد.
- اما نسبت به تست 22k، مقدار dropped به شکل قابل توجهی کاهش یافته است.

این نشان می‌دهد:
> نرخ 21k RPS نزدیک مرز ظرفیت پایدار است، ولی هنوز کاملاً پایدار محسوب نمی‌شود.

---

### ⏱️ Latency و کیفیت پاسخ
- **avg:** 11.5ms  
- **median:** 4.42ms  
- **p95:** 44.24ms ✅  
- **p99:** 75.86ms ✅  
- **max:** ~244ms  

**نتیجه:**
- از نظر latency، کیفیت پاسخ‌ها کاملاً در محدوده قابل قبول است.
- Tail latency هنوز کنترل شده و انفجاری نشده است.

---

### ❌ نرخ خطا
- **http_req_failed:** 0.00%
- تمام درخواست‌ها با status 200 پاسخ داده شده‌اند.

---

### 🧠 VU و اشباع منابع
- **VU فعال (max):** ~1309
- **VU سقف مشاهده‌شده:** ~1361
- **وضعیت CPU:** تمام هسته‌ها در طول تست ≈ 99–100%

**تفسیر مهم:**
- رسیدن CPU به 100% به معنی «حداکثر توان مصرف شده» است.
- این عدد برای **ظرفیت حداکثری** قابل قبول است، اما برای **ظرفیت پایدار Production** مناسب نیست.

---

## ✅ جمع‌بندی این تست
- نرخ 21k RPS تقریباً محقق شده ولی:
  - هنوز **dropped_iterations غیر صفر** است
  - CPU کاملاً اشباع شده

📌 بنابراین:
> **21k RPS ظرفیت حداکثری نزدیک به سقف است، اما ظرفیت پایدار محسوب نمی‌شود.**

---

## 🎯 تعریف ظرفیت پایدار (Stable Capacity)
یک نرخ زمانی «پایدار» است که همه شرایط زیر همزمان برقرار باشند:

1. **Achieved RPS ≈ Target RPS**
2. **dropped_iterations ≈ 0**
3. **error rate ≈ 0**
4. **p95 / p99 زیر SLO**
5. **CPU حدود 60–80% (دارای headroom)**

---

## 🔍 پیشنهاد گام بعدی (دقیق‌ترین نقطه پایدار)

برای پیدا کردن عددی که بتوان با اطمینان گزارش کرد:

### پیشنهاد تست‌های بعدی:
- **20,000 RPS** (دوباره، با همین duration)
- **19,500 RPS**
- **19,000 RPS**

📌 اولین نرخی که:
- dropped ≈ 0 شود
- CPU زیر ~85% بماند
- latency پایدار بماند

➡️ همان عدد را به عنوان:
> **Stable Capacity of `/hello` on this machine** گزارش کن.

---

### 📝 جمله آماده برای گزارش تیمی
> On local Ubuntu 24.04 (i7-7700, 16GB RAM), `/hello` sustained ~20.3k RPS at target 21k with 0% errors and acceptable latency (p95 ~44ms), but with non-zero dropped iterations and full CPU saturation, indicating proximity to maximum capacity rather than stable production capacity.

---

## 📌 تحلیل تست Constant-Arrival-Rate (Target: 20,500 RPS) — نتیجه و برداشت مهندسی

---

### 🧾 خلاصه اجرای تست
- **Executor:** constant-arrival-rate  
- **Target rate:** 20,500 iteration/s (≈ 20.5k RPS)  
- **Duration:** 2m  
- **Thresholds:** همگی PASS ✅  
- **HTTP errors:** 0.00% ✅  

خروجی کلیدی:
- **Achieved RPS:** **20,290.28 req/s**  
- **Dropped iterations:** **25,148** (≈ **209.56 /s**)  
- **Latency:** avg=7.95ms، p95=34.79ms، p99=62.18ms، max≈245ms  
- **VU usage:** max active VUs≈1029 (vus_max≈1256)

---

### 1) آیا نرخ هدف محقق شد؟
**تقریباً، ولی نه کامل.**
- هدف: 20,500 RPS  
- محقق‌شده: 20,290 RPS  
- اختلاف: حدود **210 RPS** (تقریباً **~1% کمتر از هدف**)

این اختلاف کوچک‌تر از تست‌های قبلی (21k/22k) است و نشان می‌دهد به محدوده پایدار نزدیک‌تر شده‌ای.

---

### 2) مهم‌ترین شاخص: `dropped_iterations`
- dropped ≈ **209.56 در ثانیه** (غیرصفر)

**تفسیر دقیق dropped در constant-arrival-rate:**
در این executor، k6 “می‌خواهد” دقیقاً با نرخ هدف iteration اجرا کند. اگر در لحظه‌هایی نتواند به موقع iteration جدید را اجرا کند (به دلیل کند شدن پاسخ‌ها، کمبود CPU، یا contention بین k6 و سرویس روی یک ماشین)، بعضی iterationها **drop** می‌شوند.

پس:
- تا وقتی dropped **غیرصفر** است، آن نرخ را نمی‌توان «ظرفیت پایدار قطعی» حساب کرد.
- اما چون dropped خیلی کم شده، این تست یک **کاندید خیلی نزدیک به ظرفیت پایدار** است.

📌 نکته مهم:
در این run به سقف VU نخورده‌ای:
- `vus_max ≈ 1256` در حالی که `maxVUs` در سناریو **3000** بوده.
پس dropped اینجا **به خاطر محدودیت maxVUs نیست**؛ بیشتر نشانه‌ی محدودیت/اشباع منابع (معمولاً CPU روی لوکال یا رقابت k6 و سرویس) است.

---

### 3) کیفیت سرویس (Latency) چطور بود؟
خیلی خوب و بهتر از runهای بالاتر:
- **p95 = 34.79ms** ✅
- **p99 = 62.18ms** ✅
- median ≈ 2.1ms (اکثر درخواست‌ها خیلی سریع‌اند)

یعنی از نظر SLOهای latency، این نرخ کاملاً قابل دفاع است.

---

### 4) خطاها
- `http_req_failed = 0%`
- همه پاسخ‌ها 200 ✅

پس bottleneck شما «fail شدن» نیست، بلکه «رسیدن دقیق به نرخ هدف بدون drop» است.

---

## ✅ جمع‌بندی این run
- شما در target=20.5k به **~20.29k RPS achieved** رسیده‌ای.
- latency و error عالی است.
- ولی چون dropped ≈ 210/s است، این نرخ هنوز **کاملاً پایدار** (stable) نیست؛ با این حال **خیلی نزدیک به stable** است.

اگر بخواهیم یک جمله گزارش‌پذیر بنویسیم:
> در constant-arrival-rate با هدف 20.5k RPS، نرخ واقعی ~20.29k RPS با 0% خطا و p95~35ms ثبت شد، اما حدود 210 iteration/s dropped مشاهده شد که نشان می‌دهد این نرخ نزدیک سقف پایدار روی سیستم لوکال است.

---

## 🎯 پیشنهاد گام بعدی برای “Stable Capacity”
برای اینکه عدد پایدار و گزارش‌پذیر داشته باشی (dropped≈0):

1) یک run با **20,000 RPS** (همان duration=2m یا بهتر 5m) بزن.
2) اگر dropped هنوز غیرصفر بود، **19,500 RPS** را تست کن.
3) اولین نرخی که:
- **dropped_iterations ≈ 0**
- Achieved ≈ Target (اختلاف خیلی کم)
- و p95/p99 داخل SLO
شود، همان را به عنوان **Stable Capacity** اعلام کن.

> نکته: چون k6 و سرویس روی یک ماشین‌اند، برای عدد “ظرفیت واقعی سرویس” بهتر است k6 را روی یک ماشین جدا اجرا کنی؛ در غیر این صورت CPU contention می‌تواند باعث dropped شود حتی وقتی سرویس هنوز توان دارد.

---