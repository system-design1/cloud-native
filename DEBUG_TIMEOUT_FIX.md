# 🔧 حل مشکل Timeout در Debug

## مشکل

وقتی در debug متوقف می‌شوید و بعد از چند خط F10 زدن، F5 (Continue) می‌زنید، response به Postman نمی‌رسد.

## دلیل

**WriteTimeout**: در `.env` شما `SERVER_WRITE_TIMEOUT=15s` است. وقتی در debug متوقف می‌شوید:
1. زمان می‌گذرد
2. اگر بیشتر از 15 ثانیه در debug بمانید
3. HTTP connection timeout می‌شود
4. Response ارسال نمی‌شود

## راه‌حل

### راه‌حل 1: افزایش Timeout در Debug (توصیه می‌شود)

در `.vscode/launch.json` تنظیمات زیر اضافه شده است:

```json
"env": {
  "GIN_MODE": "debug",
  "SERVER_WRITE_TIMEOUT": "300s",  // 5 دقیقه برای debug
  "SERVER_READ_TIMEOUT": "300s"     // 5 دقیقه برای debug
}
```

این باعث می‌شود در حالت debug، timeout 5 دقیقه باشد (به جای 15 ثانیه).

### راه‌حل 2: استفاده از Handler ساده‌تر

برای debug، از `/hello` استفاده کنید (نه `/delayed-hello`):

```bash
curl http://localhost:8080/hello
```

این handler:
- Delay ندارد
- سریع‌تر است
- کمتر احتمال timeout دارد

### راه‌حل 3: Breakpoint بعد از Sleep

اگر می‌خواهید `DelayedHelloHandler` را debug کنید:

1. Breakpoint را در **خط 92** قرار دهید (بعد از `time.Sleep`)
2. نه در خط 83 (قبل از Sleep)

این باعث می‌شود:
- Sleep اجرا شود
- بعد در debug متوقف شوید
- کمتر احتمال timeout

## نکات مهم

### 1. Timeout در Debug

در debug، همیشه timeout بیشتری نیاز دارید چون:
- شما در breakpoint متوقف می‌شوید
- زمان می‌گذرد
- Connection باید زنده بماند

### 2. استفاده از Handler ساده

برای debug معمولی، از `/hello` استفاده کنید:
- سریع‌تر
- ساده‌تر
- کمتر مشکل

### 3. DelayedHelloHandler

اگر می‌خواهید `DelayedHelloHandler` را debug کنید:
- Breakpoint بعد از `time.Sleep` قرار دهید
- یا timeout را افزایش دهید

## تست

بعد از اعمال تغییرات:

1. Debug را restart کنید (`Ctrl+Shift+F5`)
2. Breakpoint در خط 92 قرار دهید (نه 83)
3. Request بفرستید:
   ```bash
   curl http://localhost:8080/delayed-hello
   ```
4. برنامه در خط 92 متوقف می‌شود
5. F10 بزنید
6. F5 بزنید
7. Response باید برسد!

## خلاصه

✅ **مشکل**: WriteTimeout در debug
✅ **راه‌حل**: افزایش timeout در `.vscode/launch.json`
✅ **توصیه**: از `/hello` برای debug استفاده کنید
✅ **نکته**: Breakpoint بعد از Sleep قرار دهید

**موفق باشید! 🎉**

